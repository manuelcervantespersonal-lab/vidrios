# Resend (https://resend.com) — usado por /app/api/contact/route.ts
# Crea una cuenta, verifica tu dominio de envío y genera una API key.
RESEND_API_KEY=

# Correo remitente verificado en Resend (opcional).
# Si se omite, se usa "Sitio Web <onboarding@resend.dev>" (solo pruebas).
CONTACT_FROM_EMAIL="Cristalum <contacto@tu-dominio-verificado.com>"
